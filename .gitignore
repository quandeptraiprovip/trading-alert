# Dependencies
node_modules/

# Environment & secrets (TELEGRAM_*, API keys, tokens)
.env
.env.*
!.env.example

# Credentials & keys
*.pem
*.key
*.p12
*.pfx
credentials.json
secrets.json
*.secret

# Local editor / agent settings
.claude/settings.local.json
.vscode/
.idea/

# OS
.DS_Store
Thumbs.db

# Logs & runtime
logs/
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# TypeScript build output (if you add outDir later)
dist/
build/
.next/

# Bot live runtime (state persistence + trade journal)
bot-state.json
bot-state.json.tmp
trades-live.jsonl
fxdream-candidate-journal.json
fxdream-candidate-journal.json.tmp

# Kline disk cache (kline-fetch.ts)
.cache/

# Turtle live runtime (state + journal)
turtle-state.json
turtle-state.json.tmp
turtle-trades.jsonl
trading-runtime/

# Fast-trend sleeve runtime (alert-only state + journal)
fast-trend-state.json
fast-trend-trades.jsonl
fast-trend-mexc-state.json
fast-trend-mexc-state.json.tmp
fast-trend-mexc-trades.jsonl
fast-trend-mexc-runtime/

# macOS widget build output
macos-widget/TradingWidget.app/
widget-equity-history.json
